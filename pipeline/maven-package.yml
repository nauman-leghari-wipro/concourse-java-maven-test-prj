platform: linux

image_resource:
  type: docker-image
  source:
    repository: maven
    tag: 3.3.9-jdk-8-alpine

inputs:
  - name: git-repo

outputs:
  - name: jar-file
    path: target

run:
  path: git-repo/pipeline/package.sh
