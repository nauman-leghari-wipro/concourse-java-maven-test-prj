platform: linux

image_resource:
  type: docker-image
  source:
    repository: alpine
    tag: latest

inputs:
  - name: git-repo
  - name: target
  - name: some-files

run:
  path: find
  args: ["."]
